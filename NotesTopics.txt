What is @SpringBootApplication and what is inside this
What is @RestController and what is inside this
What is @MappedSuperclass and what is inside this
What is @Column(updatable = false) and what is inside this
What is @Column(insertable = false) and what is inside this
What is @Entity and what is inside this
What is @RequestMapping(path="/api", produces = {MediaType.APPLICATION_JSON_VALUE}) and what is inside this
What is Dto meaning, how mapper logic work
What is @ControllerAdvice and what is inside this
Soring boot starter validation

  @CreatedDate
    private LocalDateTime createdAt;

    @CreatedBy
    private String createdBy;

    @LastModifiedDate
    private LocalDateTime updatedAt;

    @LastModifiedBy
    private String updatedBy;



Actuator and Spring bus refresh video 87  abd 88 : When you change confi file while services are running then service doesnt detect changes
but using actuator and rabbit mq we can refresh the properties without down the microservices

spring-cloud-config-monitor
In github where config is saved =>go to setting =?webhook =>add webhook
hookdeck listen 8071 github-webhook --path /monitor

Different way that we can access the profile like qa or production


ShortCuts
🔑 List members of a class : Ctrl + F12










